this works only in your local system 
doesnt communicate through the web
and its works only for a single user

in order to execute the file
use these commands after your the directory which contains both the server file and the client file

g++ server.cpp -o server && ./server

g++ client.cpp -o client && ./client

